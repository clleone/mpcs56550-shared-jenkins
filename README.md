This repo facilitates Jenkins shared libraries for reusable pipeline code.

### Requirements
- Jenkins 2.x with Pipeline plugin
- Git

This repo contains reusable Jenkins pipeline code and is not run locally. 
It is referenced by Jenkins pipelines in each service repo.